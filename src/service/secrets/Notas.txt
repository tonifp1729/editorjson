VAMOS POR AQUÍ: 
---------------

    - Mira las fotos, hay que hacer un nuevo campo tipo select que vamos a llamar "Preguntas:" en el que marcas la opción deseada, 
    que comprenderá preguntas de tipo texto, selección y opciones. Habrá un botón al lado que trás clicarse debe de generar el tipo
    de pregunta deseada para rellenar (hay un ejemplo de la de tipo texto en las fotos que he tomado). Tambien tenemos en las fotos
    el cambio en el json que genera el formulario (hacer un array) y otra con el formato que debería de devolver el generador de JSON.